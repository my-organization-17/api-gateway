services:
  api-gateway:
    container_name: api-gateway
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 4004:4004
    env_file:
      - ./.env.docker
    networks:
      - coffeedoor-network

networks:
  coffeedoor-network:
      external: true